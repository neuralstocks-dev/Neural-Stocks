# Neural Stock Intelligence™ — PRD

Owner: Emergent Labs Inc. · Product: Neural Stock Intelligence™
Last updated: Feb 2026

---

## 1a. Recent Changes (Feb 2026)

- **Feb — Disclaimer modal mobile-viewport fix (accept button unreachable on iPhone)**: User-reported bug — the "I understand & accept" button on the legal disclaimer modal was pushed below the visible viewport on iPhone (Safari, iOS), making the gate impossible to clear without horizontal stretching or pinch-zoom. **Root cause**: `DisclaimerModal.jsx` capped only the inner disclaimer text at `max-h-[52vh]` but left the modal container itself uncapped — header (~80px) + 52vh text (~440px on iPhone 14 Pro) + checkbox area (~120px) + footer-button area (~70px) + 32px top margin pushed the bottom button off the bottom of the viewport, especially with Safari's URL bar visible. **Fix**: refactored the modal into a proper flex-column with `maxHeight: min(92svh, 92vh)` on the outer container (svh correctly accounts for mobile browser chrome on supporting browsers, vh fallback for older WebKit), `flex-1 min-h-0 overflow-y-auto` on the disclaimer text body so only that section scrolls, and `flex-shrink-0` on header / checkbox / footer rows so they pin permanently at the modal bottom. Removed the `mt-8` mobile top margin and `items-start` alignment so the modal centers vertically. **Verified live** on iPhone 14 Pro viewport (390×844) — accept button now sits at y=744px with a 51px safety gap above the viewport bottom, fully clickable. Lint clean.

- **Feb — Resend emails re-toned to match educational research positioning**: Final touchpoint of the platform-wide tone shift, completing the consistency loop across every Neulab channel. Updated `services/email.py`. **Disclaimer footer** rewritten: "Financial Disclaimer · Neulab is an AI-assisted analysis tool... not investment advice" → "Educational research output · All confidence and model-probability values describe the strength of the model's classification based on the inputs used — not forecasts of price movement or investment success... not personalized financial advice or a recommendation to buy, sell, or hold any security." Footer is reused by every email (receipts, weekly digest, future digests). **Weekly digest body** re-toned: signal rows now show `Bullish bias · 78% model probability` / `Bearish bias · 81% model probability` instead of `BUY · 78%` / `SELL · 81%`. Internal `direction` field still BUY/SELL for routing/colors. Subject line `"3 strong RF edges"` → `"3 strong directional reads"`. Empty state `"No strong edges"` → `"No strong directional reads"`. Body intro `"strongest BUY/SELL edges... before acting"` → `"strongest directional reads — framed as analytical bias, not trade instructions... before drawing conclusions"`. Auto-Scan upgrade CTA also re-toned. **Cost-anchor reminder email** (admin-only API spend tracker) intentionally left as-is — it's about Universal Key budget, not market analysis, so the educational disclaimer doesn't apply. **Verified live**: 3 test emails sent to `ai.neulab.inc@gmail.com` (Resend-verified inbox) — populated weekly digest with 3 signals (NFLX bullish, TSLA bullish, BRMS.JK bearish), receipt email (showcases new disclaimer footer), empty-state weekly digest. All delivery results: True. Lint clean.

- **Feb — Telegram alerts re-toned to match educational research positioning**: Final touchpoint of the platform-wide tone shift. Re-wrote alert composition at all 4 sites — `routers/analysis.py` AI-verdict signal alerts, `routers/analysis.py` pattern alerts, `services/auto_scan.py` RF watchlist scan, `routers/telegram.py` test endpoint, plus the `services/telegram.py` link-confirmation message. **Title format**: `"BUY · NFLX · 78%"` → `"Bullish bias · NFLX · 78% classification strength"`. Bias mapping: BUY→Bullish, SELL→Bearish, HOLD→Neutral (used for color routing internally; framed as analytical bias in user-facing copy). **Body format**: replaced "Target: $X" with "Scenario level: $X". Every alert closes with italicized disclaimer: `"Educational research output — confidence/model probability is classification strength based on the inputs, not a forecast probability. Not personalized financial advice."` Pattern alerts now say "candlestick pattern observed" instead of "BULLISH Bullish Engulfing detected" — observational not imperative. RF scans clarify the model probability isn't a forecast and direct users to "Open the app... before drawing conclusions" instead of "before acting". **Verified live**: 4 test messages sent to `@joelord69` Telegram via real bot infrastructure — base test, AI verdict (NFLX bullish 78%), pattern (BRMS.JK Bullish Engulfing), RF scan (TSLA 72%) — all delivered successfully with the new educational tone. Lint clean.

- **Feb — Educational research repositioning across LLM prompt + Web report + PDF + Public share page**: User repositioned the platform from "trading recommendations" to "educational research tool" to align tone with sample report they provided. Changes mirror exactly across all four surfaces. **LLM prompts** (`services/ai.py`) — re-toned all 3 modes (Standard, Candlestick, Hybrid) with explicit "CRITICAL TONE" sections instructing Claude to use observational language ("the model classifies X as bullish") instead of imperative ("you should buy"), to frame `confidence_score` as classification strength NOT forecast probability, to refer to `price_target`/`stop_loss` as "illustrative scenario level" / "invalidation level" instead of trade instructions, and to always present at least one alternative interpretation. Two new optional schema fields added: `alternative_scenarios {bullish, bearish, neutral}` and `what_could_change_view: [string, ...]`. Existing fields kept for backward compat. **Web report** (`AnalysisReportPage.jsx`) — quoted-style amber-bordered "EDUCATIONAL RESEARCH REPORT" banner above verdict (model-generated summary, not financial advice, confidence is classification strength), "Analytical bias · classification, not a trade instruction" subtitle below BUY/SELL/HOLD chip, "Scenario level" replaces "Price Target" with footnote, "Invalidation level" replaces "Stop Loss" with footnote, "Risks to the current interpretation" replaces "Risk Factors" with lead sentence, new "Alternative scenarios" module (bullish/neutral/bearish color-coded panels), new "What could change the view" bullet list, new canned "How to read this report" legend (Analytical bias / Confidence / Scenario level / Invalidation level / Horizon). **PDF** (`services/pdf.py`) — same banner at top of report, same subtitle below verdict, "Scenario level / Invalidation / Horizon" replaces "Target / Stop / Horizon" line, same retitled risks section, alternative scenarios as colored-bordered tables, what-could-change-view bullet list, how-to-read legend block. **Public share page** (`PublicVerdictPage.jsx`) — full parity: educational banner, analytical bias subtitle, relabeled level boxes with footnotes, retitled risks, alternative scenarios, what-could-change-view, how-to-read legend. New optional fields exposed on `_public_view`. **Backward compat**: Alt-scenarios + WCCV render only when LLM produced them (older analyses pre-Feb-2026 silently degrade — no broken sections). **Verified end-to-end**: NFLX live web report shows all new modules; PDF generation 200/11KB confirms layout intact, `analyze_file_tool` visual diff confirms all 5 verifiable elements (banner, subtitle, scenario/invalidation labels, retitled risks, how-to-read legend). Lint clean both ends. Live tests 32/32 PASS.

- **Feb — Top IDX Confluences leaderboard module on dashboard**: Quality score was a passive page-level signal — promoted to an active dashboard ranking. **Backend** — new `GET /api/idx/top-confluences?limit=3&days=7`. Filters user's analyses to `.JK` tickers in the past N days, projects only the headline fields, dedupes to one row per ticker (latest analysis wins), sorts by `quality_score DESC` with ties broken by `created_at DESC`. Returns `{window_days, scanned, unique_tickers, items}`. Ranking + dedup logic extracted into pure `_rank_top_confluences()` helper for fast unit testing without Motor/FastAPI plumbing. **Frontend** — new `<TopConfluencesModule />` component placed below the Watchlist+Alerts row on the dashboard. Renders a 3-row leaderboard with rank chevron · BULLISH/BEARISH/DIVERGENCE direction badge · ticker + company · hero quality score in serif (color-graded by tier) · INSIDER FILED freshness + price column · ArrowUpRight chevron (entire row links to `/analysis/{ticker}`). **Silent on no-data** — users with zero IDX analyses in the past 7 days never see the module, so US-only traders aren't shown an empty IDX ranking. **Tests**: `/app/backend/tests/test_top_confluences.py` — 7 unit tests covering quality DESC sort, latest-per-ticker dedup, limit truncation, recency tiebreak, payload completeness, defensive zero-score, empty input. **Caught a real bug during testing**: the original sort used `(-qs, created_at)` which inverted the tiebreak (older sorted first); fixed to `(qs, created_at)` with `reverse=True`. **Live verification**: `BRMS.JK 93/excellent → BBCA.JK 55/moderate → ELSA.JK 41/moderate` rendered correctly on dashboard. **32/32 confluence tests pass** (7 new + 25 existing). Lint clean both ends.

- **Feb — Confluence quality score (0–100) + tier badge**: Confluence detection was binary (fires or doesn't) — but a same-day strong-accumulation × 3-pattern confluence is structurally far stronger than an 80-day mild × 1-pattern one even though both pass the binary trigger. Shipped a multiplicative quality scorer: `quality_score = round(100 × freshness_factor × regime_factor × count_factor × direction_factor)`. **Factors**: (a) `freshness_factor = max(0, 1 − age_days / 90)` linear decay; (b) `regime_factor` = strong:1.0, mild:0.7; (c) `count_factor` = 1pat:0.7, 2pat:0.85, 3+pat:1.0; (d) `direction_factor` = confluent:1.0, divergence:0.5. **Tier mapping**: ≥80 excellent, ≥60 strong, ≥40 moderate, <40 weak. **Backend** — `_confluence_quality()` helper in `routers/analysis.py`, injected into all 4 confluence return paths (bullish/bearish/divergence×2). `_is_bandarmology_stale` refactored on top of new pure `_bandarmology_age_days()` helper so age + staleness now share one parser. **Frontend** — `<ConfluenceChip>` renders a color-graded `QUALITY 93/100 · EXCELLENT` mono-font badge in the chip header next to BULLISH/BEARISH/DIVERGENCE label, with hover tooltip explaining the freshness contribution (`"Filed 6 days ago — high freshness weight"`, etc.). **Back-fill**: applied scoring retroactively to 11 existing IDX confluences in db.analyses; 4 stale (>90d) confluences correctly auto-removed via the upstream gate (KOBX×2 etc). Live distribution: BRMS 93/excellent (6d), BBCA 55/moderate (32d), FUTR 24/weak (59d) — the score curve correctly discriminates. **Tests**: extended `test_bandarmology_staleness.py` with 8 quality-score regressions (excellent-tier sanity, age-degrades-score, divergence<confluence, strong>mild, more-patterns-higher, tier band invariants, all-factors-present). **25/25 PASS**. Lint clean both ends.

- **Feb — Confluence engine stale-data gate (>90 day threshold)**: Original nudge premise (Claude sees bandarmology data) turned out to be wrong on inspection — `services/ai.py` never passes `bandarmology` to the LLM. But the audit surfaced a worse bug: `_compute_confluence()` in `routers/analysis.py` paired candlestick patterns with `bandarmology.regime` to produce "Double-confirmation: bullish reversal pattern + smart-money accumulation" labels persisted on the analysis doc and shown to users — **regardless of how stale the bandarmology data was**. So for KOBX.JK with a 4-year-old "strong_accumulation" regime + a fresh bullish candlestick, we were fabricating a fake "double confirmation" off dead data. Shipped `_is_bandarmology_stale(bandarmology, threshold_days=90)` — a Python parser mirroring the frontend's `parseFilingDate()` ("DD MMM YY" → days-since helper) — and short-circuited `_compute_confluence()` to return `None` when stale. Defensive: malformed/missing data is treated as fresh (we never silently suppress confluences for valid current data). **Tests** locked in at `/app/backend/tests/test_bandarmology_staleness.py` — 17/17 PASS covering threshold boundary (89d not-stale / 91d stale), bullish + bearish + divergence suppression paths, and 8 malformed-input variants. Backend regression suite still 58 PASS / 1 SKIP. No frontend change needed — the visual de-emphasis layer (shipped previously) and the data-layer suppression layer now agree on the same 90-day cutoff.

- **Feb — Bandarmology stale-data warning system (>90 day threshold)**: User concern: tickers with no recent insider filings (e.g. KOBX.JK with 14 Jun 22 latest disclosure — 3.9 years old) were rendering the "Smart Money Flow" module at full visual fidelity, misleading users into acting on dead signals as if they were live institutional flow. Shipped a tiered de-emphasis system in `BandarmologyCard.jsx`. **Threshold**: 90 days since latest filing. **Visual treatment when stale**: (a) amber `STALE · LATEST FILING N{d|mo|y} AGO` chip with `<AlertTriangle>` icon directly under the regime title; (b) muted `Historical snapshot.` lead paragraph above the accumulation bar explicitly stating no new disclosures in 90+ days and clarifying the percentages reflect what insiders did, not what they're doing now; (c) regime BUY% chip flipped from BUY-green/SELL-red/HOLD-amber to muted gray; (d) accumulation bar + secondary stats grid de-emphasized to 0.55 opacity. **Refactored** `filingAgeBadge()` → split into pure `parseFilingDate()` (returns `{date, days}` or null) + `formatAge(days)` so the staleness check and the existing freshness subtitle both share one parser. **`data-testid`s**: `bandarmology-stale-chip`, `bandarmology-stale-explainer`, `bandarmology-accumulation-wrap`. **Verified end-to-end**: KOBX.JK (3.9y old) renders the full stale treatment with all four de-emphasis layers; BBCA.JK (1mo old) renders the healthy state with no chip, no explainer, full-opacity colored bar — clean discriminator.

- **Feb — Public share page parity for Verdict Accuracy v2 calibration block**: User reported the public `/v/{share_id}` page was missing the calibration audit trail that the verdict's owner sees in their web report. Shipped full parity. **Backend** — extended `_public_view()` in `routers/analysis.py` to expose `calibration_version`, `confidence_score_pre_calibration`, `confidence_adjustments`, `rf_disagreement_penalty`, `earnings_gate_applied`, `days_until_earnings`, and a whitelisted `rf_opinion` projection (`prob_up`, `prob_down`, `horizon_days`, `edge` only — no internal feature payload leaks). **Frontend** — new `<PublicCalibrationBlock>` component inside `PublicVerdictPage.jsx`, sitting between the verdict snapshot section and the Reasoning section. Two visual states mirror the owner's web report: (a) **fired** (hold/amber border) — "POST-LLM CALIBRATION · CONFIDENCE ADJUSTED" overline, "Verdict Accuracy v2" serif title, monospace score breakdown `LLM raw 78 → −12 → 66 final` (red delta chip + green final), bullet list of adjustments, dotted-rule separator + RF probabilities/Krauss-Do-Huck citation OR earnings-window cap explainer for whichever rule fired; (b) **clean** (buy/green border) — "NO ADJUSTMENT NEEDED" overline + clean sentence stating both gates ran clean. Read-only by design — no interactive drawer (the deep-dive lives at `/technical#confidence-calibration`). **Verified end-to-end**: NFLX share `/v/114a51da3c30` renders the fired state with full breakdown 78 → −12 → 66 + Krauss citation; AAPL share `/v/37eea4761a44` renders the green clean state. Backend response confirmed including all 7 calibration fields. `data-testid`s: `public-calibration-block`, `public-score-breakdown`, `public-calibration-bullets`. Lint clean both ends.

- **Feb — Trade Slip · Pro/Elite shareable one-page PDF**: Built a paid-tier viral asset that turns every verdict into screenshotable trading-channel content. **Backend** — `services/pdf.py::generate_trade_slip_pdf(analysis, share_url)` renders a 5.5 × 7.5 in portrait single-page slip: NEULAB overline + analysis date · big serif TICKER + company · big colored verdict + confidence% · `VERDICT ACCURACY V2` calibration breadcrumb (score breakdown when fired, green "raw output" when clean) · 4-cell ENTRY / TARGET / STOP / HORIZON levels strip with gold rule above · italic THESIS one-liner (first sentence of executive_summary, capped at 220 chars) · up to 3 KEY DRIVERS bullets (prefers calibration adjustments, then risk_factors) · footer with NEULAB branding + share URL + micro-disclaimer. New endpoint `GET /api/analysis/{analysis_id}/trade-slip` gates on `effective_plan_key(user) == "free"` → 402 with friendly upgrade copy; admin / test-unlock / Pro / Elite / Day-Pass → 200. Endpoint auto-mints (or reuses) a public share record in `db.shared_verdicts` so the slip's footer URL leads to a real `/v/{share_id}` page — built-in viral loop. **Frontend** — new `<Receipt>` "Trade Slip" button on `/analysis/{TICKER}` action row between Share and Export PDF, with `data-testid="download-trade-slip-button"`. Hover title: "One-page shareable trade slip — perfect for screenshots into Discord/Telegram". **Verified end-to-end**: NFLX (fired state, 78→66 calibration breadcrumb visible, 4.1KB) and AAPL (clean state, green "no calibration adjustment fired" line, 4.1KB) both render single-page with `analyze_file_tool` confirming all 10 layout checks pass; Free user → 402, Pro user → 200 (3.3KB), Admin → 200; lint clean both ends.

- **Feb — PDF parity for Verdict Accuracy v2 calibration block**: User requested the calibration audit trail — that the web report shows in the Score Breakdown drawer — also appear in the exported PDF so it survives forwarding to brokers / pasting into trade journals. Shipped `_append_calibration_block()` in `services/pdf.py` (lines 84–198). Two visual states mirror the web UI: (a) **fired state** — amber-bordered box, "POST-LLM CALIBRATION · CONFIDENCE ADJUSTED" overline, score-breakdown line `LLM raw 78 → −12 → 66 final` in monospace with red delta + green final, bullet list of each adjustment string, academic source citation per fired rule (Krauss/Do/Huck 2017 for RF-disagreement; earnings-window cap explainer for earnings gate); (b) **clean state** — green-bordered box, "POST-LLM CALIBRATION · NO ADJUSTMENT NEEDED" overline, single sentence: "Earnings-proximity gate and RF-disagreement penalty both ran clean. Confidence shown is Claude's raw output — no adjustment applied." Both states close with a muted footer pointer to `neulab.xyz/analysis/{TICKER}` for the deep-dive. Block renders only when `calibration_version == "v2"` or when `confidence_adjustments` exists. Container is a single-cell ReportLab Table with `LINEBEFORE` for the colored left border and `BOX` for a subtle outer rule. **Verified** end-to-end on the live deployment: NFLX (fired, 78→66) and AAPL (empty-state) both rendered cleanly via `/api/analysis/{id}/pdf`, `analyze_file_tool` visual diff confirmed the amber + green states, score breakdown legible, no overflow/clipping, layout intact across all 3 pages.

- **Feb — V2 cost revert + admin-only cost dashboard at `/admin/cost`**: User flagged that V2's expanded data context (60 daily + 26 weekly closes) was burning ~70% more credits per verdict (~$0.046 vs the original ~$0.027) without commensurate accuracy gain. Reverted #1 (data context — the only cost driver) while keeping #2 (post-LLM calibration — earnings gate + RF disagreement penalty + score breakdown drawer — these add zero LLM tokens). **`services/ai.py`**: standard/hybrid prompt payload now sends 20 daily closes (V1 baseline) instead of 60 daily + 26 weekly. Server still fetches weekly history because the candlestick scanner needs it, but raw weekly closes no longer enter Claude's prompt. **Result**: ~40% reduction in input tokens per verdict. Costs on $0.027/verdict pricing model = ~2.7 credits each.
  - **Admin Cost Dashboard at `/admin/cost`**: per-day verdict count × cost table (last 7/30/60/90 days), 4 lifetime stat cells (verdicts, USD spent, credits burned, avg burn/day), SVG sparkline of daily verdict counts, plus a **Universal Key balance anchor**: admin enters their last-known top-up balance (e.g. 54.89 credits), backend persists with timestamp, UI subtracts estimated spend since the anchor to project remaining credits + verdicts left. Low-balance warning fires below 10 verdicts remaining.
  - **Backend**: new `GET /api/admin/cost/summary?days=N` (admin-only) returns `{daily, totals, cost_per_verdict_usd, balance_anchor}`. New `POST /api/admin/cost/balance-anchor` to upsert the anchor in `db.app_settings._id="credit_balance_anchor"`. Constant `COST_PER_VERDICT_USD = 0.027` defined centrally so frontend + backend agree on the rate. Verified end-to-end with admin user `jolor69@gmail.com`.
  - **Public-facing `/technical#how-neulab-handles-load`**: cost cell + cost-paragraph mention are now **admin-only** via `useAuth().user.is_admin`. Reasoning: per-call cost is operational data — useful for the operator, but exposing it publicly leaks margin info to competitors and primes users to think in per-call terms ("this click costs you 3¢") which is the wrong mental model for a subscription product. Grid auto-collapses 5 → 4 cells for non-admins.
  - **Banner copy fix**: `LLMBudgetBanner` now correctly states "~4.6 credits / $0.046 per verdict" and "100 credits ($1) ≈ 22 verdicts" matching real economics (was incorrectly stating ~$0.01 / 500 verdicts before).
  - **Tests**: fixed pre-existing flaky `test_earnings_gate_caps_high_confidence_inside_window` (off-by-one when running later in the day — `_earnings_in_days` was using `.date().isoformat()` which stripped the time component). Now uses full ISO timestamp. **17/17 backend pytest pass.**

 User asked for two things: (1) show WHICH pipeline stage is active during an analysis instead of a featureless "Thinking…" spinner, and (2) show how the Final Score was concluded (LLM raw → calibration adjustment → final). Shipped both.
  - **Backend**: `start_analysis()` now seeds a `progress` field on the `analysis_jobs` doc with `{phase: "queued", phases: [...mode-conditional...], completed: []}`. New `_set_job_phase(job_id, phase)` helper is called at every stage boundary inside `_create_analysis_impl_inner` (`fetching_data` → `computing_technicals` → `scanning_patterns` (only candlestick/hybrid) → `llm_thinking` → `rf_scoring` → `calibrating` → `done`). Best-effort writes — never fail the analysis.
  - **Backend**: `verdict_calibration.calibrate_verdict()` now snapshots `confidence_score_pre_calibration = verdict["confidence_score"]` BEFORE any rule fires. Mirrored into the persisted `db.analyses` doc. Allows the UI to show the LLM's raw confidence vs. the post-calibration final.
  - **Frontend**: New `<AnalysisProgressStepper progress={progress} />` component renders 5–6 horizontal pills (mode-conditional) with three visual states: completed (green + check icon), active (amber + spinner), pending (muted gray + dot). Mounted on `AnalysisReportPage` next to the Re-analyze + queue chip row, only visible while `analyzing && progress`. Polling loop in `runAnalysis` updates `progress` state on each tick.
  - **Frontend**: Score-breakdown row inside the V2 calibration breadcrumbs module — `LLM raw 78 → −12 → 66 final` rendered in monospace with the −delta in a sell-colored chip. Only shown when `confidence_score_pre_calibration !== confidence_score` so unchanged verdicts don't show redundant `78 → 0 → 78`.
  - **Verified** by `testing_agent_v3_fork` iter 39: 17/17 backend pytest pass (10 existing + 7 new in `test_iter39_progress_and_breakdown.py`), 3/3 frontend checks pass, NFLX hybrid integration round-trip confirms `pre=78 → penalty=12 → final=66` end-to-end.

- **Feb — Candlestick mode card copy upgrade**: Updated `/technical` Mode B body from generic *"Best for swing/short-term timing"* to explicit *"Best for swing/short-term timing where you trust price action over the fundamental story (e.g., trading reversals, ignoring noisy earnings windows)"* — gives users a concrete *when to pick this mode* hint.

 User asked for a "Stocks queued: N concurrent · ~estimated wait Xs" chip next to Re-analyze. Shipped end-to-end. **Backend**: (1) global `asyncio.Semaphore(ANALYSIS_CONCURRENCY=4)` wraps `_create_analysis_impl` so this worker runs at most 4 full-pipeline analyses concurrently — excess requests queue rather than saturating the loop. (2) `GET /api/analysis/queue/status` (public, no auth) returns `{capacity, running, queued, avg_duration_s, estimated_wait_s, is_busy}` reading from `_QUEUE_SNAPSHOT` (refreshed every 1s by a background task). (3) **Critical**: Claude calls now run inside their own OS thread + own asyncio loop via `_run_chat_in_thread()` + JSON parsing via `asyncio.to_thread(_parse_ai_json)` — verified the main asyncio loop stays responsive (queue-status returns in 100ms even under 6-concurrent saturation, was timing out at 8s before). **Frontend**: new `<AnalysisQueueChip />` polls every 5s, hides when `is_busy:false` (zero noise during quiet periods), renders amber chip "Pipeline busy · N running · M queued · ~Xs wait" with hover title. Mounted next to Re-analyze on `/analysis/{TICKER}` and inside the analyze-mode-section on `/dashboard`. **Verified** end-to-end via testing_agent_v3_fork iter 38 (initial saturation issue caught) → iter 39 fix verified: 6 concurrent NFLX/TSLA/AAPL/MSFT/NVDA/GOOGL hybrid analyses → queue-status returns in 88-138ms (was 8s+) showing `running:4 queued:2 is_busy:true estimated_wait_s:50`.

- **Feb — Technical page transparency upgrade**: User asked for "why this matters" reasons under each `WE DO NOT USE` and `WE DO USE` bullet on `/technical`. Shipped: `MythLi` component now accepts a `reason` prop, every list item (6 negatives + 7 positives = 13 items) has a short justification underneath in muted grey explaining WHY the choice matters. Negatives explain the failure mode of each rejected ML approach (CNN can be fooled by chart styling, LSTMs can't survive regime shifts, FinBERT has stale 2018 vocabulary, etc.). Positives explain the strength of each pick (Claude reasons in plain English, RSI/SMA are replicable in Excel, RF gives free feature importance, etc.). Goal: turn the methodology page from "what we do" into "what we do AND why it gives you a defensible edge".

- **Feb — RF section beginner explainer + YouTube video**: User wanted an easy intro to Random Forest before the methodology dump. Shipped a green-bordered "New here? Start with this 90-second primer" module at the TOP of `#random-forest` containing: (1) plain-English "imagine asking 400 independent analysts" analogy, (2) 5-bullet "why this is the right model for stock direction" list (hard-to-overfit, naturally probabilistic, free feature importance, doesn't pretend to time the market, battle-tested in finance research with Krauss 2017 + Gu/Kelly/Xiu 2020 NBER citations), (3) embedded responsive 16:9 YouTube iframe at `https://www.youtube.com/embed/gkXX4h3qYm4` (IBM Cloud "What is Random Forest?" 5-minute visual walkthrough). Iframe wrapped in 56.25%-padding div so it stays correct on every viewport.

- **Feb — Onboarding wizard CTA label fix**: User confused by "Continue · 0/3" — looked like progress. Replaced with: 0 picks → "Pick at least 1 stock" (disabled), N picks → "Continue with N stocks" / "Continue with 1 stock". Top step indicator (1 → 2 → 3) unchanged; that was already correct.

- **Feb — Verdict Accuracy V2 breadcrumb empty-state**: User couldn't see V2 in action on quiet verdicts. Backend `calibrate_verdict()` now always seeds `confidence_adjustments=[]` + `calibration_version="v2"`. Frontend renders the breadcrumb module on every V2 verdict — green positive empty-state when no rule fires ("Verdict Accuracy v2 · no calibration needed — confidence shown is Claude's raw output"), amber state when adjustments fired with bullets + `How?` link to `/technical#confidence-calibration`. Verified live: NFLX re-analysis in hybrid mode shows "RF model disagrees (strong edge, 80% down) — confidence reduced by 12 points (was 78, now 66)".

- **Feb — P0 fix: 504 Gateway Timeout on stock re-analysis (NFLX)**: Two converging causes: (a) the legacy synchronous `POST /api/analysis/{ticker}` blocked for the full 50–60s pipeline → ingress 30s cap → 504; (b) the frontend polling loops aborted on a single transient 5xx from the polling endpoint. **Fix**: deprecated endpoint now delegates to start-and-poll (returns job_id in 87ms); polling tolerates up to 5 consecutive 5xx errors with `/latest` fallback. **Verified** by testing_agent_v3_fork iter 37.

- **Apr 25 — Onboarding pack #1 (3 items)**: (1) **Marketing-claims regression test** at `/app/frontend/src/__tests__/marketing-claims.test.jsx` — 13 jest tests run in <0.5s, read canonical plan limits from `src/__fixtures__/plan_limits.json` (synced from backend `core/config.py` via `/app/scripts/sync_plan_limits.py`) and grep marketing pages for the exact numbers. Caught and fixed 5 marketing-claim discrepancies: WhyUsPage final-CTA `3 watchlist + 1 analysis/day` → `5 + 3`; PublicTryVerdictPage `unlimited analyses` → `5-stock watchlist`; PublicTryVerdictPage `5 watchlist · 50 analyses / day` (50 wasn't even a real plan number) → `5 + 3 / day`; rate-limited fallback teaser `unlimited` → `3 analyses per day`; TryNowBox placeholder `sign up for unlimited` → `sign up for 3 analyses/day`. All copy now matches backend enforcement and is regression-locked. (2) **`/manual` user manual page** — beginner-friendly walkthrough with 10 numbered sections + TOC + final CTA. Explicitly calls out the two beginner-critical claims the user requested verbatim: "the watchlist does **NOT** auto-update" warn callout in the watchlist section, and "alerts only fire when the AI's confidence is **at least 75%**" warn callout in the alerts section. Added to main nav + mobile drawer; Protected route; wraps `<AppShell>` like every other authenticated page. (3) **`<FirstTimeManualPill />`** on Dashboard — book-icon nudge between TestUnlockBanner and the quota banner that auto-hides when `quota.analyses_this_week >= 1` OR the user clicks X / "Read it →". Storage key versioned (`sai_manual_pill_dismissed_v1`) so we can re-promote the manual on a later cohort. **Verified** by testing_agent_v3_fork iter 31, 32, 33: 7/7 manual-page PASS + 5/5 first-time-pill PASS. Marketing-claims jest 13/13 still green.

- **Apr 25 — P0 fix: `is_admin` reads from DB returning False for admin endpoints**: User `jolor69@gmail.com` reported on production (https://neulab.xyz) that the Daily RF pre-filter Telegram push toggle was still disabled with the upgrade prompt, even after the iter 29-30 frontend defensive override shipped. **Root cause discovered during audit**: `is_admin` is a derived flag computed by `get_current_user()` based on email matching `ADMIN_EMAILS` env, NEVER persisted to the DB. But three backend endpoints — `GET /api/auth/me/auto-scan`, `POST /api/auth/me/auto-scan`, `GET /api/auth/me/weekly-digest` — were calling `db.users.find_one(...)` then reading `u.get("is_admin")` from the raw DB record (always returning `False`). For admin `jolor69@gmail.com` the eligibility check still passed because plan="elite" matched the paid-plan list, but if any admin email had plan="free" they would have been wrongly locked out. Same bug existed in three background services: `services/auto_scan.py` (Mongo `$or {"is_admin": True}` filter), `services/rf_retrain.py` drift-alert candidate query, and `services/weekly_digest.py` per-user gating. **Fix**: switched all 6 sites to either (a) read from the `user` dict returned by `Depends(get_current_user)` which has the live derived flag, or (b) for background services that don't have an auth context, query users by email against `ADMIN_EMAILS` allow-list directly. **Also hardened frontend** in `SettingsPage.jsx`: added `refreshUser()` call on mount so cached user objects with stale `is_admin` get refreshed; extended `effectiveAutoScan` override to also accept `test_unlock_expires_at` as a paid-plan signal. **Verified end-to-end**: admin `/me/auto-scan` returns `plan_eligible: true`, admin `/me/weekly-digest` returns `is_paid: true`, free user regression intact (`plan_eligible: false`).

- **Apr 25 — Mobile UX pack #2 (3 items)**: (1) **Mode selector mobile clipping** — switched to `block sm:flex` parent + `grid grid-cols-3 w-full sm:flex` button group + short labels (`Std`/`Candle`/`Hybrid`) below sm: breakpoint via dual-span pattern; ★ recommended marker hidden on mobile. (2) **iPhone notch / Android cutout safe-area** — added `paddingTop: env(safe-area-inset-top)` to the sticky header in `AppShell.jsx`, plus `paddingTop` + `paddingBottom` env() insets to the mobile drawer panel so the close button doesn't collide with the status bar. `viewport-fit=cover` already set in index.html. (3) **Auto-Scan client-side defensive eligibility override** — `SettingsPage.jsx` derives `effectiveAutoScan.plan_eligible = autoScan.plan_eligible || planIsPaid` from `useAuth().user` so admin/Pro/Elite/Day-Pass/test-unlock users NEVER see a wrongly-locked card even if the API response is stale.

- **Apr 25 — P0 fix: Analyze spinner missing on watchlist (disclaimer race)**: Reported by user — non-admin clicks "Analyze" on a freshly-added watchlist stock, no progress circle appears. **Root cause**: `useDisclaimer().ensureAccepted()` in `DisclaimerModal.jsx` previously gated on `accepted === true`. On a fresh page load `GET /disclaimer` is in-flight and the local `accepted` state is `null`; clicking Analyze before it resolved made the gate fail, opened the disclaimer modal, and never set `analyzingTicker` — so no spinner ever rendered. If the user dismissed the modal, the queued action was lost. **Fix**: changed the predicate to `accepted !== false` — null and true both run the action optimistically. Backend still enforces via `428 disclaimer_required`, and the existing `promptFromError(err)` catch path opens the modal in that case. Net: zero-latency spinner for repeat users (~419ms first-paint for demo@stockai.io vs the >2.5s API round-trip), correct gating for never-accepted users. **Verified** by `testing_agent_v3_fork` iter 27: 3/3 frontend PASS — non-admin analyze, admin analyze, AnalysisReportPage autorun-via-View-button — refreshing-pill rendered, NO modal interruption.
- **Apr 25 — P0 verify: Daily RF pre-filter Telegram sample push delivered**: User asked to confirm Auto-Scan toggle was ON for admin and to see the actual Telegram message format. Backend was already correctly persisting `auto_scan_enabled=true` for `jolor69@gmail.com`; the previous null `last_run_at` was just because the scheduler's 09–22 UTC window hadn't yet fired with the cooldown reset. Manually triggered `auto_scan._scan_user(admin)` via `/tmp/send_rf_sample.py` after unsetting `auto_scan_last_run_at`. Two real RF strong-edge alerts pushed to admin's Telegram (chat 6031779675 / @joelord69): TSLA BUY 65% ($376.30) and FUTR.JK SELL 34% (Rp 224). Format reads: `RF watchlist scan · {DIR} · {TICKER} · {N}% \\n\\n Random-Forest model alone flags {TICKER} · last close {P} as {DIR} over a 20-day horizon with probability {N}% \\n\\n ⚠️ This is a lightweight RF-only alert — NOT a full Neulab analysis... Mode: Auto-scan (RF only) · model hist. accuracy 48% \\n Open {TICKER} in app →` (with magic-link auto-login token appended).

- **Feb — ConfidenceDot on Dashboard Signal Feed**: Personalizes every alert row with a small colored dot (green/amber/red/grey) reflecting the user's *personal* historical win-rate for that alert type, pulled from the existing `GET /api/alerts/stats` endpoint — zero new backend. Thresholds: GREEN ≥60% win rate + ≥3 decisive trades, AMBER 45–60% + ≥3 decisive, RED <45% + ≥3 decisive, GREY <3 decisive (default initial state). Tooltip on hover via native `title` attribute — e.g. *"AI Verdict: you're 75% on this channel — high confidence, follow the signal."* vs *"Pattern Scan: only 38% historical hit-rate for you — cross-check carefully before acting."* New "Confidence · Triage →" link also added in the Signal Feed header (deep-links to `/alerts`). Non-blocking fetch — dashboard never waits on stats. Graceful degradation if stats endpoint fails (dots stay grey, `console.warn` logged). **Verified** by `testing_agent_v3_fork` iteration 16: 100% frontend pass (7/7 checks), 18 dots render correctly across admin's signal + pattern alerts, GREEN/AMBER/RED color paths validated via mocked stats response, mobile 390px viewport clean, zero console errors, regression clean (mark-all-read + alert deep-links unchanged).

- **Feb — Smart Alert Triage (`/alerts`) · model-vs-market scoreboard**: Turned the passive `db.alerts` collection into an active scoreboard. **Backend**: extracted alert endpoints out of the oversized `routers/analysis.py` into a dedicated `routers/alerts.py` (9 endpoints: GET/filter, mark-read, read-all, star, archive, take, resolve-pending, stats). New per-alert fields `starred`, `archived`, `taken`, `taken_at`, `entry_price`, `outcome`, `outcome_pnl_pct`, `outcome_resolved_at`, `exit_price`. Marking an alert as "taken" snapshots the current yfinance close as `entry_price` and records `taken_at`; after `RESOLVE_HORIZON_DAYS=20` (same as default RF horizon), a lazy resolver (fires on every `GET /alerts/stats`) pulls the forward close and classifies `win` / `loss` / `break_even` (|P&L| < 0.5% band). P&L is inverted for SELL signals so shorts are graded correctly. `GET /alerts/stats` returns a per-type scoreboard (`by_type.signal`, `by_type.pattern`, `by_type.rf_watchlist_scan`) with taken / wins / losses / win-rate % / avg P&L % — so users can see *which channel of the platform is pulling its weight for their style*. **Frontend**: new `AlertsPage.jsx` at `/alerts` with (1) hero copy "Your model-vs-market scoreboard", (2) 4-KPI row (Taken · Win rate · Avg P&L · Break-even), (3) per-channel breakdown table sorted by win-rate with color-coded cells (≥60% green, 45-60% amber, <45% red), (4) 5 filter tabs (Inbox / Unread / Starred / Taken / Archived) with URL-independent tab state, (5) alert cards with left-border colored by signal, Star / Took-it / Archive action buttons, ticker autorun deep-links, and outcome badges (COOKING amber for pending, WIN green, LOSS red, FLAT grey). `Took it` button hidden for HOLD signals since they have no directional P&L. Nav link added between Portfolio and Scorecard (desktop + mobile drawer). **Verified** by `testing_agent_v3_fork` iteration 15: **24/25 backend pytest PASS** (1 skip — admin has no HOLD alert for the rejection test) + **100% frontend** (18 alert cards, 5 filter tabs, star/archive/take flows, zero console errors). Regression suite at `/app/backend/tests/test_alerts_triage.py`. Two minor polishes applied post-test: (a) renamed shadowed `alert` parameter in `onTake` to `a` to avoid clash with `window.alert`, (b) added `loadErr` state + `[data-testid='alerts-load-error']` banner so stats/list fetch failures don't silently blank the page.

- **Feb — Weekly RF Digest email (retention + upgrade hook)**: New Sunday-evening recap that emails every opt-in user the top-N BUY/SELL edges the Random-Forest model sees on their watchlist. **Architecture**: `services/weekly_digest.py` reuses auto-scan's `_fetch_history` + `_fetch_market_context` helpers for cost efficiency, then ranks signals by `|P−0.5|` descending (min-edge gate = 0.10). Free users get **top 3** + 2 blurred/locked slots with an upgrade CTA pointing to Auto-Scan/Pro; Pro/Elite/Daypass/Admin get **top 5** fully unblurred. Scheduler loop fires Sundays 22–23 UTC (≈ 5–7pm ET), 6h wake cadence, per-user 150h cooldown to survive restarts. **Email template** (`services/email.py::_weekly_digest_html`) matches the existing `_receipt_html` aesthetic — dark-mode, Cormorant Garamond heading, monospace signal rows with BUY/SELL color coding, clickable `autorun=1` deep-links to `/analysis/{ticker}`, unsubscribe link pointing to `/settings`, and the same financial disclaimer footer. Subject line branches on `signals` (empty-state gets its own subject). **Endpoints**: `GET/POST /api/auth/me/weekly-digest` (open to ALL plans — no 402 — since this is the retention funnel; deliberately different from Auto-Scan's gated access). **Frontend**: new Weekly Digest card on `/settings` between Auto-Scan and Email modules, with shadcn Switch, "top 3 vs top 5" copy that branches on `is_paid`, `weekly-digest-upgrade-link` to `/pricing` for Free, and live stats (last digest timestamp + signal count) once enabled. **Verified** by `testing_agent_v3_fork` iteration 14: 10/10 backend pytest + 100% frontend pass. End-to-end dry-run confirmed the full RF-scoring + email-send pipeline works (1 user due, 1 email delivered via Resend). Regression suite at `/app/backend/tests/test_weekly_digest.py`.

- **Feb — Watchlist Auto-Scan (Option C · RF pre-filter + Telegram) shipped**: Once a day, the scheduler loop (started in `server.py` @startup, 4h polling cadence, 09–22 UTC quiet-hours guard, 20h per-user cooldown) walks every Pro/Elite/Day-Pass/admin user who has (a) `auto_scan_enabled=true` AND (b) a linked Telegram chat. For each ticker on their watchlist, it pulls 1y yfinance OHLCV, computes the Random-Forest `feature_row_for_today()`, scores with `rf_predictor.predict_from_features()`, and **only** fires when `edge=="strong"` (|P−0.5|>0.15) — a BUY if P≥0.65 or SELL if P≤0.35. Zero Claude LLM calls (cost profile: ~0 $/day for 100 users × 20 tickers). Pushes both a dashboard `alerts` row and a Telegram push with explicit "RF-only, not a full Neulab verdict — tap Analyze for the multi-lens report" disclaimer. **Backend surface**: `GET /api/auth/me/auto-scan` → `{enabled, telegram_linked, plan_eligible, last_run_at, last_alerts_sent}`; `POST /api/auth/me/auto-scan` → `{enabled:bool}` with a 402 gate for Free and 400 gate for "no Telegram linked". **Frontend surface**: new Watchlist Auto-Scan card on `/settings` (between Telegram and Email modules) with shadcn `Switch`, plan-locked state that deep-links to `/pricing`, "link Telegram first" hint for Pro users without a chat, and live stats (last run timestamp + alerts sent in the last run) once enabled. **Verified end-to-end** by `testing_agent_v3_fork` iteration 13: backend 6/6 pytest assertions PASS (correct shape for admin/free, enable+disable persists, 402 for free, 400 for Pro-without-Telegram, scheduler imports clean); frontend 3/3 UI checks PASS (admin module renders + toggle flips + persists, free user sees plan-locked + upgrade link + disabled toggle, zero console errors). Regression suite at `/app/backend/tests/test_auto_scan.py`.

- **Feb — Email Reply-To updated**: `SENDER_EMAIL=onboarding@resend.dev` with `EMAIL_FROM_NAME=Neulab` (brand display preserved in inbox From-column) and `EMAIL_REPLY_TO=ai.neulab.inc@gmail.com` (new dedicated Gmail for user replies). DNS verification on `neulab.xyz` still pending — once complete, revert `SENDER_EMAIL` to `noreply@neulab.xyz`.

## 1b. Recent Changes (Apr 2026)

- **Apr 24 — Fix: IDX analysis 504 timeout (BRMS.JK reproducible bug)**: User reported `BRMS.JK` analysis consistently failing on production with no verdict generated. Root-caused to **30s production ingress streaming cap vs. the full IDX pipeline** (yfinance history + RapidAPI quote/keystats/bandarmology + SPY/VIX market-context + Claude LLM call + RF prediction) occasionally exceeding that budget — same ingress timeout class of bug that was previously fixed for the anonymous "Try one free" flow by converting it to 202 polling. Reproduced directly: `POST /api/analysis/BRMS.JK?mode=standard` on prod returned **504 stream timeout** at exactly 30.1s. **Fix mirrors the existing `/analysis/quick/{kind}` pattern**: (1) new endpoint `POST /api/analysis/{ticker}/start?mode=…` returns `{job_id, status:"running"}` immediately after synchronously enforcing disclaimer/quota/mode-validation, then spawns `_run_single_analysis_job()` as an `asyncio.create_task` kept alive in `_BG_TASKS`. Background job has a 120s wall-clock budget and writes its result or error back to new `db.analysis_jobs` collection. (2) new polling endpoint `GET /api/analysis/jobs/{job_id}` returns the job doc (includes `result` when `status=="done"` or `error`/`status_code` when `failed`). (3) TTL index on `analysis_jobs.started_at_ts` expires completed/failed jobs after 1 hour (mirrors `anon_try_jobs` pattern); registered at server startup via `_ensure_analysis_indexes()` hooked into `@app.on_event("startup")`. (4) Frontend `DashboardPage.analyzeOne()` and `AnalysisReportPage.runAnalysis()` switched to start-and-poll: POST `/start`, poll every 2.5s up to 180s, show `failed` error with server-provided detail, timeout message if poll window exhausts. Legacy `POST /api/analysis/{ticker}` synchronous route preserved for backward-compat (used by curl/tests and any external consumers). **Verified end-to-end**: curl roundtrip = start→done in ~6s (3 polls); browser hit pattern `POST /start(200) → GET /jobs/{uuid}(200)`, button state correctly flips `Re-analyze → Thinking… → Re-analyze`, verdict renders (BRMS.JK HOLD 45% with full exec summary). Zero JS console errors. All lint-clean. Prod redeploy needed to ship the fix.

- **Apr 24 — PayPal webhook unblocked**: Received new Live Webhook ID `2DD10119B06470237` from user; updated `PAYPAL_WEBHOOK_ID` in `backend/.env`, restarted backend, verified diagnostic card flips to HEALTHY with all 8 curated events subscribed (BILLING.SUBSCRIPTION.{ACTIVATED,CANCELLED,SUSPENDED,EXPIRED,PAYMENT.FAILED} + PAYMENT.SALE.COMPLETED + CHECKOUT.ORDER.APPROVED + PAYMENT.CAPTURE.COMPLETED). Historical 7 events still show "unverified" as expected (signed with old webhook ID); next real renewal/cancellation will confirm verified=yes loop organically.

- **Apr 24 — Backtesting (Option C + Option D) — "honest track record"**: Shipped a disciplined two-layer backtest page at `/backtest` that turns Neulab's verdicts into a hypothetical portfolio vs. SPY (or ^JKSE for IDX-majority users) — the feature users have been asking for since launch. **(1) Live user backtest (Option D)** — `services/backtest.py` + `routers/backtest.py` — walks each user's `analyses` history chronologically, simulates equal-weight long-only entries at the next available daily close after each BUY, exits on matching SELL or a 30-day calendar timeout, marks-to-market daily. Pro/Elite gated (402 for Free). Cached in `db.backtest_runs` kind="live" with 6h freshness + auto-invalidate on new verdict. Selects benchmark = SPY or ^JKSE based on whether >50% of verdicts are `.JK`. Returns full payload: trades[], equity_curve[], benchmark_curve[], metrics{total_return_pct, benchmark_return_pct, alpha_pct, win_rate_pct, max_drawdown_pct, trade_count, window_start, window_end, …}, strategy_note. `/api/backtest/me/status` is a lightweight eligibility check used by the Dashboard to decide whether to surface a call-out. **(2) Neulab-ML walk-forward (Option C)** — `scripts/compute_ml_backtest.py` standalone script + `/api/backtest/ml` public read endpoint. Monthly-rebalance, top-5 of 25 mega-caps by Random-Forest P(up), equal-weight, walk-forward from the RF training cutoff (2025-05-27) to today. Cached in `db.backtest_runs` kind="ml" keyed on `rf_trained_at` — auto-invalidates on RF retrain via new `_recompute_ml_backtest_async()` hook in `services/rf_retrain.py` (fire-and-forget after drift check). Admin can manually retrigger via `POST /api/admin/backtest/ml/recompute`. **Live numbers on first compute: +17.63% strategy return vs SPY +21.22% = -3.58% alpha over 11 months, 52.7% win rate, -11.72% max drawdown.** The honest underperformance is intentional: it's the transparent story that matches the RF holdout accuracy (47.8% vs 50.4% baseline) already on `/technical#random-forest`. **(3) `BacktestPage.jsx`** renders both layers with a prominent disclaimer module at the top ("Illustrative — not a forecast · ignores commissions, slippage, taxes, dividends"), a 4-KPI row (total return / alpha vs benchmark / win rate / max drawdown), a dual-line equity curve chart (Neulab vs benchmark with distinct amber/green colors + dashed benchmark), a trade log table with entry/exit prices + exit reason, and the same structure duplicated for the ML walk-forward section below. Pro gate card + upgrade CTA for Free users; empty-state with "run 3 verdicts to unlock" for new accounts. Nav link added between Scorecard and Pricing (plus mobile drawer variant). **Verified end-to-end by `testing_agent_v3_fork`** — 10/10 backend pytest assertions PASS (402 gating, cache hit, force_refresh bypass, public /ml endpoint, admin 403 for non-admin + started for admin) and 9/9 frontend checks PASS (KPIs render with live numbers, gate + upgrade CTA for Free, ML section public for both tiers, nav link desktop + hamburger, zero horizontal overflow at 390x844). Only follow-up flagged: same Recharts width(-1)/height(-1) console warning family from iteration 11 — fixed in this commit by adding `minWidth:1/minHeight:1` to both ResponsiveContainer wrappers. All Python + JS lint clean.

- **Apr 24 — Installable PWA (Add to Home Screen)**: Neulab is now installable as a native-feeling app on iOS, Android, and desktop Chrome/Edge. **(1) Web App Manifest** at `/public/manifest.json` with `name: "Neulab · Neural Stock Intelligence"`, `short_name: "Neulab"`, `start_url: "/dashboard"` (launches straight into the dashboard, not marketing), `display: "standalone"` (no browser chrome), `background_color: "#0A0C10"`, `theme_color: "#0A0C10"`, and 3 app-shortcuts — Dashboard, Portfolio, Scorecard — that appear on long-press of the home-screen icon on Android. **(2) Icon set** generated at `frontend/public/` — `icon-192.png`, `icon-512.png`, `icon-maskable-192.png` (10% safe-zone), `icon-maskable-512.png`, `apple-touch-icon.png` (180×180), `favicon-32.png`. Design: dark `#0A0C10` rounded-square with hairline axes, white line-chart glyph trending up, green accent dot at the high point, NEULAB wordmark. Generated via `/tmp/gen_icons.py` using PIL + DejaVu Bold. **(3) `index.html` meta**: added `<link rel="manifest">`, `<link rel="apple-touch-icon">`, `<link rel="icon" type="image/png" sizes="32x32">`, corrected `theme-color` from `#000000` → `#0A0C10`, added `viewport-fit=cover` for iPhone notch, `apple-mobile-web-app-capable="yes"`, `apple-mobile-web-app-status-bar-style="black-translucent"`, and `apple-mobile-web-app-title="Neulab"`. **(4) `InstallAppCard` component** (`components/InstallAppCard.jsx`) with three render branches driven by runtime detection — Chromium browsers listen for `beforeinstallprompt`, cache the deferred event, and trigger the native prompt on click via `deferredPrompt.prompt()` + `userChoice`; iOS Safari renders a 3-step "Share → Add to Home Screen" expandable walkthrough (since Apple doesn't fire the event); standalone sessions (detected via `matchMedia('(display-mode: standalone)')` + legacy `navigator.standalone`) render a muted "App installed" confirmation. Surfaced on `/settings` under the Email module. Cleanup on unmount removes both `beforeinstallprompt` and `appinstalled` listeners. **Verified**: manifest fetch returns 200 with all 5 icons + 3 shortcuts parsed correctly. Chromium variant of the install card renders on desktop Chrome (install button + graceful "not ready yet" hint when engagement threshold isn't met). iOS Safari UA-spoofed variant correctly swaps to the 3-step iOS instructions card (`install-card-ios` → Steps 01 Share / 02 Add to Home Screen / 03 Confirm name & Add). All lint-clean.

- **Apr 24 — Mobile UX audit & responsive fixes (P1)**: Resolved the user's long-standing complaint about hidden menus on mobile. **(1) Mobile hamburger menu added to AppShell**: the desktop nav was previously `hidden md:flex` with **no mobile replacement**, leaving mobile users with zero way to navigate between Dashboard / Portfolio / Scorecard / Pricing / Why us / Technical / Admin / Settings. Shipped a proper right-side drawer — hamburger button (`data-testid="mobile-menu-button"`) appears below `md` breakpoint, opens a full-height drawer with backdrop blur, "Signed in as" header with plan/admin badges, all 7 nav links + Admin link (conditional on `user.is_admin`) + Settings, and a Log-out button at the bottom. Drawer auto-closes on route change via `useEffect(()=>{setMobileOpen(false)},[loc.pathname])`, traps body scroll while open, and uses a CSS keyframe `slideInRight` (220ms ease-out) for the panel entrance. Every interactive element has `-mobile` suffixed test IDs. **(2) Header decluttered on mobile**: user email (`hidden lg:inline` + `max-w-[160px] truncate`) and desktop logout button (`!hidden md:!inline-flex`) removed from mobile bar — only logo, theme toggle, and hamburger remain (3 items = clean). Used Tailwind `!` important prefix to override `.btn-ghost { display: inline-flex }` base CSS. **(3) Page-level horizontal overflow killed**: added `html, body { overflow-x: hidden }` in `index.css` to prevent any accidental horizontal scroll on mobile from nested tables/grids (Admin page was reporting `scrollWidth=912` on a 390px viewport before this fix). Also added `.module { min-width: 0 }` and `.overflow-x-auto { max-width: 100% }` so internal tables stay scrollable within their own container rather than bubbling to body. **(4) Analysis Report ticker header**: `flex items-baseline gap-4` bumped to `flex-wrap gap-x-4 gap-y-1 min-w-0 break-all` so long company names don't overflow next to the hero ticker symbol (clamp lowered to `2.2rem` floor). **Verified on 390px mobile viewport (iPhone 14 Pro reference)**: Dashboard, Admin, Portfolio, Pricing, Scorecard, Technical, Settings, Analysis Report — every route reports `canScrollX: False` + `hamburger: True`. Drawer navigation round-trip validated: `nav-portfolio-mobile` click → `/portfolio` → drawer auto-closed. Desktop (≥md) layout completely untouched — logout button, full nav, and email badge re-appear at `md`+.

## 1b. Recent Changes (Apr 2026)

- **Apr 24 — Finnhub-for-IDX silenced + RapidAPI/Bandarmology credited across pages**: **(P0)** `services/yfinance_svc.get_quote()` now skips the Finnhub `/quote` call entirely for `.JK` tickers via `is_idx_ticker()` guard — Finnhub free tier doesn't cover IDX, so the call was only producing harmless 403 log spam (caught by `return_exceptions=True` but still logged). After restart: zero new Finnhub 403 log entries on IDX analyses. Also saves ~1 outbound HTTP call per IDX verdict. **(P1)** IDX data-source crediting swept across 4 surfaces: (a) `AnalysisReportPage` data-sources footer now branches on `ticker.endsWith('.JK')` — IDX variant credits RapidAPI (primary) + yfinance (fallback/OHLC) + Bandarmology + CNBC Indonesia + Detik Finance RSS + Bahasa sentiment; US variant unchanged. (b) `TechnicalPage` pipeline stage "Data acquisition" rewritten to describe the IDX RapidAPI path (quote + keystats + bandarmology caches). (c) `TechnicalPage` data-sources table expanded from 10 to 13 rows — added "Live quote (IDX) · RapidAPI", "Fundamentals — IDX · RapidAPI keystats", "Bandarmology · /emiten/{sym}/insider", "Top IDX Picks scanner · /main/trending" with their coverage + cache columns. (d) `TechnicalPage` Honest-limits IDX bullet rewritten to flip the narrative from "IDX has less than US" to "IDX gets two exclusive things US doesn't — Bandarmology + Top Picks". Stack footer mentions RapidAPI IDX alongside yfinance + Finnhub. **(PDF)** `services/pdf.py` footer now emits a third variant for IDX reports — credits RapidAPI primary / fallback per `idx_data_source`, adds Bandarmology line when `analysis.bandarmology` is present, swaps Finnhub news for CNBC Indonesia + Detik Finance. **Verified** via `testing_agent_v3_fork` iteration 11 — 6/6 frontend checks PASS end-to-end on `tiers@demo.io`: clicked Top IDX Picks → ENRG.JK row → autorun fired → verdict rendered with `idx-source-chip-rapidapi` + Bandarmology + Confluence + RF modules + correct IDX-aware data-sources footer with ZERO Finnhub mentions.

- **Apr 23 — Multi-point polish pack (autorun × 3 entry points + admin quota reset + WhyUs Bandarmology rows + Scorecard empty-state explainer)**: wrapped up 4 UX refinements. **(1) Autorun deep-links** — now appended to three more entry points so every path into the verdict page is zero-click: (a) Dashboard watchlist "View" buttons, (b) Telegram alerts — `send_alert_to_user()` gained an optional `ticker` kwarg that appends "Open {TICKER} in app →" HTML link using new `PUBLIC_APP_URL` env var; both AI-verdict alerts and candlestick alerts now pass it, (c) the public share-verdict page's "Run your own on {TICKER}" secondary CTA. The existing autorun effect safely no-ops on repeat arrivals with an existing recent verdict. **(2) Admin per-user quota reset**: new endpoint `POST /api/admin/users/{id}/reset-quota` stamps `quota_reset_at = now`. `enforce_analysis_quota`, `quota_snapshot`, and `enforce_idx_analysis_quota` all honour the floor — analyses older than the reset timestamp stop counting against the window but stay on the user for the scorecard. Admin UI has a Timer-icon button in every user row with a confirm dialog. `_sanitize_user()` returns `quota_reset_at`. **(3) Why Us — IDX differentiator rows**: 3 new rows at the top of the comparison table (Bandarmology, Candlestick × Bandarmology confluence, Top IDX Picks) marked ✓ for Neulab and — for all 7 competitors. Each has an amber "EXPLAIN" button opening a rich dialog with full methodology + deep-link to `/technical#bandarmology`. WhyUsPage is now stateful. **(4) Scorecard empty-state explainer**: prominent amber module on `/scorecard` when all verdicts are pending. Answers "why is this empty after one week?" — explains the 7/30/90-day thresholds, 4-cell stats grid (Min age · Hit threshold · You have · Still cooking), 3-step "What you need to do" list ending with "Nothing else — the market resolves verdicts automatically." Verified showing "You have 63 verdicts cooking. None are 7 days old yet." on `tiers@demo.io`.

- **Apr 23 — Bandarmology × Candlestick confluence + RF drift alerts + IDX marketing + hard-limit reminder correction**: Shipped 4-part polish pass. Confluence chip cross-references candlestick patterns against bandarmology regime (bullish / bearish / divergence). RF drift detection runs after every retrain, pushes Telegram alert if acc drops ≥5pp or Brier rises ≥20% vs 4-week rolling mean. IDX marketing banner on `/pricing` + new `/technical#bandarmology` public explainer section (methodology, formulas, 5 regime thresholds, honest caveats, confluence overlay). Admin IdxBudgetCard hard-limit reminder corrected — clarified that RapidAPI's hard-limit setting is provider-controlled (not consumer-togglable on this API), linked to Subscriptions & Usage page instead, documented the "remove payment card" nuclear option as real belt-and-braces defence.

## 1. Original Problem Statement

Build an AI Stock Analysis Platform (Phase 1 MVP):
- Institutional-grade AI reasoning accessible to retail investors
- Watchlist-based workflow with AI verdicts (BUY/SELL/HOLD)
- Tiered monetization (Free / Pro / Elite)
- Public share verdicts + transparent accuracy scorecard
- Admin console for user management + pricing control
- PayPal subscriptions + Resend email receipts

## 1a. Recent Changes (Apr 2026)

- **Apr 23 — Bandarmology × Candlestick confluence + RF drift alerts + IDX marketing**: wrapped up a 4-part polish pass on top of the RapidAPI + Bandarmology launch. **(1) Candlestick × Bandarmology confluence chip**: new `_compute_confluence()` in `routers/analysis.py` cross-references the candlestick scanner's detected patterns against the bandarmology regime on every hybrid IDX analysis. Fires a prominent banner on the verdict page (`ConfluenceChip` component) with three variants: `bullish` (green — bullish pattern + accumulation regime), `bearish` (red — bearish pattern + distribution), or `divergence` (amber — signals pull opposite ways). Trigger relaxed from "combined bias strong" to "any pattern + regime" per user intent. BBCA.JK hybrid analysis correctly fires "BULLISH CONFLUENCE · STRONG · Double-confirmation: bullish reversal pattern + smart-money accumulation · Bullish Harami + Inverted Hammer + 88% insider accumulation". Attached to `doc.confluence` so PDFs and shared verdicts carry it too. **(2) RF drift alert via Telegram**: new `_check_drift_and_alert()` in `services/rf_retrain.py` runs after every successful retrain (scheduler + admin + CI). Queries last 4 successful retrains for rolling means of `holdout_accuracy` and `calibrated_brier`, compares against the new model. Drift triggers: accuracy drops ≥5pp OR Brier rises ≥20%. When triggered, pushes an HTML-formatted Telegram message (model age, universe, new vs rolling-avg metrics, reasons list, "reload or retrain" CTA) to every admin with a linked `telegram_chat_id`. Outcome (whether drift fired or not) is persisted on the retrain-job doc under `drift_check` for ops history. Uses the existing `send_alert_to_user()` helper from `services/telegram.py`. **(3) IDX marketing angle**: new differentiator banner on `/pricing` ("NEW · FOR IDX INVESTORS — The only platform showing live director-level accumulation signals for the IDX") that deep-links to a new `/technical#bandarmology` public explainer section. The explainer documents: methodology, formulas (`accumulation_ratio`, `smart_money_ratio`, `foreign_net_shares` formulae in monospace), the 5 regime labels and their thresholds, honest caveats (insider reporting lag, sub-threshold holders missing, not-a-forecast), and the confluence overlay. SEO-ready copy usable as LinkedIn / IDX-forum acquisition hook. **(4) Hard-limits reminder on admin IdxBudgetCard**: when `RAPIDAPI_KEY` is configured, the card now renders an amber second-defence reminder explaining how to set a hard monthly cap on the RapidAPI developer dashboard (with direct link) as backup to our 950/mo soft cap. All 4 changes lint-clean on Python + JS.

- **Apr 23 — IDX Bandarmology + Top Picks (Pro feature layer)**: shipped two flagship IDX-exclusive features built on the live RapidAPI key. **(1) Bandarmology smart-money flow**: new `get_bandarmology()` in `services/idx_rapidapi.py` consumes `/api/emiten/{sym}/insider` (a goldmine endpoint we discovered — returns per-insider BUY/SELL action, change in shares, director/commissioner/major-shareholder badges, nationality). We compute the accumulation score **locally** from the raw filings rather than using a separate "accumulation detector" endpoint — saves budget (1 call instead of potentially 4), makes the math fully auditable, and survives provider endpoint changes. Signals exposed: `accumulation_ratio` (buy ÷ total shares), `smart_money_accumulation` (directors+commissioners+major holders only), `foreign_net_shares`, `buy_count` vs `sell_count`, regime labels (strong/mild accumulation, balanced, mild/strong distribution, no_signal), and the 5 most-recent filings with sample detail. Surfaced on every IDX verdict via new `BandarmologyCard` component — red/green accumulation bar + 3-cell stats grid (smart money %, foreign net flow, total filings) + recent-filings list with SMART MONEY / FOREIGN badges. BBCA.JK currently renders "Strong accumulation · 88% BUY · 19 buys / 1 sell · 7.31M shares bought by directors" — exactly the kind of insight retail IDX platforms don't expose today. **(2) Top IDX Picks (Pro/Elite gated)**: new `get_top_picks()` ranks the `/api/main/trending` feed by a multi-factor score (0.6 × capped daily % change + 0.4 × price-quality heuristic that penalises sub-Rp 100 penny stocks). Cached 30 min server-side so opening the dialog costs at most 1 RapidAPI req per half-hour. New endpoint `GET /api/idx/top-picks?limit=10` with plan gating — Free users get HTTP 402 with upgrade CTA. New `IdxTopPicksDialog` component + 6th Quick-Action button on the Dashboard (grid bumped from 5→6 cols). Tapping any row deep-links to `/analysis/{ticker}` for a full AI analysis. **Rate-limit hardening**: bumped `_MIN_INTERVAL` from 1.05s to 1.3s (30% safety margin — provider's per-second counter is strict and 1.05s tripped 429s under parallel `asyncio.gather` calls), and added one automatic retry on 429 with 2s backoff. **Verified live**: BBCA.JK analysis returns `regime: "strong_accumulation"` with 20 parsed filings; Top Picks returns 10 ranked IDX names (KOBX +34.43%, MAXI +33.96% etc.); RapidAPI usage counter at ~12 requests after testing — well within 950/month budget.

- **Apr 23 — RapidAPI IDX integration (BASIC free tier · augment yfinance)**: added `services/idx_rapidapi.py` that wraps three endpoints from `indonesia-stock-exchange-idx` (yasimpratama88) on RapidAPI — `/api/emiten/{sym}/info` (quote snapshot), `/api/emiten/{sym}/keystats` (flattened ratios — real path has no hyphen, corrected after discovery), `/api/analysis/technical/{sym}` (RSI/MACD/SMA signals). For `.JK` tickers the analysis router now fetches these in parallel with yfinance; the RapidAPI values overlay the yfinance quote + fundamentals when present (richer: PE/PB/EPS/ROE etc. in IDR). yfinance is kept for OHLCV history (free, unlimited) and the RSS news scraper stays unchanged. **Budget protection**: BASIC plan is 1,000 req/month with **$0.01/req overage**, so we enforce a soft budget of **950 req/month** (~5% safety margin) via a MongoDB counter (`db.rapidapi_usage.{month: "YYYY-MM"}`). When exhausted, `_call()` returns `None` and the caller silently falls back to yfinance. **Per-tier daily caps** on `.JK` analyses: Free 3/day, Pro 10/day, Elite 30/day, Day Pass 10/day. **Rate limiting**: 1.3s min interval (see above), asyncio Lock guarded. **UI**: subtle "IDX LIVE" / "YF FALLBACK" chip on verdict header, admin `IdxBudgetCard` on `/admin` with usage bar + 4-step RapidAPI sign-up instructions when key isn't set. New endpoint `GET /api/admin/rapidapi/usage`. **Env**: `RAPIDAPI_KEY`, `RAPIDAPI_IDX_HOST`, `RAPIDAPI_IDX_MONTHLY_BUDGET=950`. Caches: quote 10m, technical 30m, fundamentals 24h, bandarmology 1h, trending 30m. User's live key paste verified end-to-end: BBCA.JK analysis flipped from `yfinance` to `rapidapi` source with live PE=13.77, PB=2.81, budget counter incrementing correctly.

- **Apr 23 (v4) — Automated RF retrain pipeline (FastAPI + GitHub Actions)**: productionised the RF model lifecycle so the calibrator stays in-distribution on an ongoing basis. **Backend infrastructure**: new `services/rf_retrain.py` orchestrator — spawns `scripts/train_rf.py` as a detached subprocess, streams stdout into a Mongo `rf_retrain_jobs` collection (log-tail rolling update every 5s), atomically swaps `rf_signal.joblib` on success via `os.replace`, then calls `rf_predictor.reload()` to hot-load the new weights without a backend restart. Guarded by an asyncio Lock + filesystem lock so parallel triggers coalesce into the single running job. New endpoints under `/api/admin/rf/*`: `GET /status` (current job + model age + stale flag), `POST /retrain` (admin-triggered retrain, idempotent), `POST /reload` (pick up an out-of-band model drop, e.g. from CI). **Weekly scheduler**: FastAPI `@app.on_event("startup")` spawns `weekly_retrain_loop()` — every 6 hours it checks `rf_signal.meta.json` `trained_at`; if >7 days old (configurable via `RF_RETRAIN_STALE_DAYS` env var) and no job is running, it fires `trigger_retrain(triggered_by="scheduler")`. **Training-script hardening**: `_download_market_context()` now retries 3× with 2/4/8s backoff and falls back to `^GSPC` when `SPY` is intermittently 404'd by yfinance (~1-in-10 call rate). Atomic write pattern: dumps to `rf_signal.tmp.joblib` + `.meta.tmp.json`, then `os.replace()` — inference never sees a half-written file. **GitHub Actions nightly**: `.github/workflows/rf-retrain.yml` runs every Monday 04:00 UTC (and on-demand via `workflow_dispatch`). Installs pinned deps, runs training, sanity-checks the saved bundle (size >1MB, proba shape correct on a zero vector), publishes a formatted Actions summary with holdout metrics + top-5 feature importances, and uploads the joblib + meta.json as a 30-day retention artifact. **Admin UI**: new `RFRetrainCard` on `/admin` — shows status chip (Healthy / Stale / Running / Failed), live metadata grid (trained_at · universe · holdout acc · calibrated Brier), collapsible log-tail of the last run, and two action buttons (Retrain now / Reload from disk). Polls `/rf/status` every 6s while a job is running so the log streams live. **Verified end-to-end**: triggered a retrain via `POST /api/admin/rf/retrain`, watched `rf_retrain_jobs` doc transition `running → success` with live log-tail, confirmed `trained_at` on `/api/analysis/rf-model/meta` changed (predictor reloaded), UI card updated in real-time.

- **Apr 23 (v3) — Regime-aware features + isotonic calibration**: added two market-regime features to the RF pipeline — `spy_ret_20d` (SPY 20-day trailing return = market momentum) and `vix_level_rel` (VIX today ÷ 252-day mean − 1 = regime fear level). Retrained on 346 tickers with the expanded 23-feature set. Results confirm the regime features dominate: **VIX level 28.4% importance · SPY 20d 14.6% · SMA20/SMA50 6.6% · ATR/close 5.2%**. OOB score jumped **0.583 → 0.635** (in-distribution learning meaningfully improved by regime conditioning). 2025 holdout accuracy 47.75% (vs 50.42% baseline) — the regime shift between training and 2025 holdout still kills forward-generalisation, exactly as the previous iteration showed, but for a more instructive reason now. Wrapped the base RF with **`CalibratedClassifierCV(FrozenEstimator(base), method="isotonic")`** fit on a held-out 20% slice of the training window (62,834 rows). Brier score comparison surfaced honestly on `/technical`: **calibrated 0.2749 vs uncalibrated 0.2559** — on this particular regime-drifted holdout the uncalibrated model is *slightly* better calibrated, so `/technical#random-forest` shows an "Honest note" explaining that the calibrator was fit on 2024 data whose probability landscape differs from 2025. We keep the calibrated wrapper shipped because it produces smoother, less step-function probabilities and future retrains on fresher data should restore the calibration benefit. Provenance banner on every verdict now reads "… · isotonic calibrated" when calibration is present. Infrastructure changes: `services/features.py` gained a `market_df` kwarg on `compute_feature_frame` / `feature_row_for_today`; `routers/analysis.py` added a TTL-cached (10 min) SPY+VIX snapshot helper that feeds every RF inference call; `services/rf_predictor.py` now reads `feature_importances` from the bundle top-level since `CalibratedClassifierCV` doesn't expose the attribute directly; `scripts/train_rf.py` downloads SPY+VIX once, passes through feature extraction, trains base model, calibrates with FrozenEstimator, saves bundle with pre-computed feature importances + calibration metadata. New endpoint fields: `calibration_method`, `calibration_rows`, `calibrated_brier`, `uncalibrated_brier`, `uncalibrated_accuracy`, `uncalibrated_auc`.

- **Apr 23 — RF retrain on wider universe + 20-day horizon + provenance banner**: Random Forest retrained from scratch on a much broader slice of the S&P 500 (~344 tickers across all 11 GICS sectors, up from 165) using a longer **20 trading-day (≈1 calendar month) forward-return horizon** (up from 5 days). Training window 2021-10-12 → 2026-04-22, walk-forward cutoff 2025-05-27, 312k train rows / 78k holdout rows. Honest 2026 holdout metrics: accuracy 47.70%, ROC-AUC 0.471, OOB 0.583 vs always-majority baseline 50.53% — model **underperforms** the baseline by 2.83pp on holdout (OOB 58% shows in-distribution learning, confirming regime drift on the post-cutoff window). Shipped the new artifact anyway as a transparent skeptic layer; `/technical#random-forest` renders the live "Honest reading: underperforms baseline by 2.83pp" call-out automatically from `rfMeta`. **New model-provenance banner** on every verdict's Secondary Opinion module: "Predicted by a Random Forest trained on 344 tickers · 2021–2026 · 20-day forward horizon. Past behaviour is not indicative of future results — this is a probabilistic opinion, not a price forecast." Banner text is fully dynamic from `model_info` (training_start_date, training_end_date, universe_size, horizon_days) — future retrains will auto-update the copy without frontend edits. Honest-disclosure footer also now shows the full training window and walk-forward cutoff. Backend changes: `scripts/train_rf.py` expanded universe, horizon CLI arg (default 20), saves `training_start_date`/`training_end_date` to meta. `services/rf_predictor.py` passes those fields into the inference payload. `routers/analysis.py` `/api/analysis/rf-model/meta` exposes them to the UI. `components/RandomForestOpinion.jsx` renders the provenance banner above the title. `pages/TechnicalPage.jsx` "Live holdout metrics" header now shows "Training window: 2021-10-12 → 2026-04-22 · horizon 20 trading days · walk-forward cutoff 2025-05-27". **Superseded by Apr 23 (v3) retrain — see above entry.** Model artifact: `/app/backend/models/rf_signal.joblib` (52 MB).

- **Apr 23 — Random Forest secondary-opinion layer (first trained ML component)**: scikit-learn `RandomForestClassifier(n_estimators=400, max_depth=12, min_samples_leaf=50, class_weight='balanced')` trained on 5 years × 165 S&P 500 large-caps using a strict walk-forward split. Predicts binary "5-day forward return positive?" from 21 deterministic features (returns, RSI, SMA ratios, realized vol, volume, MACD, candle shape, 52-week regime). Shipped services: `services/features.py` (shared extractor used by both training and inference), `services/rf_predictor.py` (lazy-load runtime with graceful no-op), `scripts/train_rf.py` (offline retrain script). Wired into `routers/analysis.py` — each verdict now returns `rf_opinion` alongside the LLM verdict. New `RandomForestOpinion.jsx` module on the verdict page shows: direction + probability bar, top-3 variables the model relied on (deep-linked to the relevant `/technical` anchors), agree/disagree chip vs LLM, and honest holdout metrics footer. When `|prob−0.5| < 0.08` the UI explicitly renders "No meaningful edge" rather than a misleading number. `/technical#random-forest` documents the model with live metrics from `GET /api/analysis/rf-model/meta` (holdout accuracy, ROC-AUC, OOB score, baseline, top-10 feature importance). **Superseded by Apr 23 retrain — see above entry.** Never used for position sizing; informational only.

- **Apr 23 — `/technical` page (engineering transparency)**: new deep-dive that honestly documents what Neulab uses (Claude Sonnet 4.5 + deterministic indicators + rule-based pattern engine + keyword heuristic sentiment) and what it does NOT (SVM, Random Forest, CNN, LSTM, FinBERT, proprietary datasets). Eleven sections: hero, myth-buster, 7-stage pipeline, confidence-score mechanics (4 signal families × 5 confidence bands), indicator formulas (RSI / SMA / MACD / volume ratio) replicable in Excel, all 15 candlestick patterns with bias chips + rule summaries, 3 analysis modes, 10-row data-source attribution table, 6 feature specs, honest-limits disclosure, full stack footer. Linked in top nav as "Technical".

- **Apr 22 — Promo discounts + Day Pass one-time tier**:
  - **Promo discount engine** — admin can set independent % discounts on Pro-monthly and Elite-monthly (plus optional label like "Launch Week"). Stored in `db.settings.pricing`; live PayPal plans are auto-rotated to the discounted monthly price on save. Non-admin Pricing page shows a promo banner + per-card strikethrough + `SAVE X%` badge + label.
  - **Day Pass (one-time $5)** — new 4th tier in `PLANS["daypass"]` with admin-editable price, duration (days), and quotas (analyses/day, analyses/week, watchlist, shares/day). Quick batch sweep intentionally disabled; Standard + Candlestick + Hybrid + Pattern Scan always on.
  - Backend: `POST /api/billing/daypass/order` (creates PayPal Order v2 CAPTURE intent) + `POST /api/billing/daypass/capture` (captures + sets `user.plan="daypass"` + `daypass_expires_at`, auto-reverts to free on expiry via `quota.effective_plan_key`). Receipt email sent via Resend. Audit trail in `db.daypass_orders`.
  - `services/paypal.py` extended with `create_order` / `capture_order` / `get_order`.
  - `services/pricing.py` + `routers/admin.py` refactored to support promo fields (`promo_pro_discount_pct`, `promo_elite_discount_pct`, `promo_label`, `promo_ends_at`) and Day Pass config (`daypass_price`, `daypass_duration_days`).
  - Frontend Pricing page — 3 PayPal button variants render for Day Pass (Buy Now / Pay Later / Debit or Credit Card). Banner shows when the user has an active pass (with expiry date).
  - Admin console — new "Promo discount (monthly)" block (Pro % / Elite % / Label) and "Day Pass · one-time purchase" block (price / duration). Tier limits table now includes a fourth `Day Pass` row with an extra `Watchlist` column.
  - Live validation: activated 20% Pro / 15% Elite promo + $5 / 7-day Day Pass → live PayPal plans rotated, Order v2 created with real order_id (`0A428550DT000811T`), Pricing page screenshots confirm strikethrough + SAVE badges + full Day Pass card render.

- **Apr 22 — IDX (Indonesia Stock Exchange) support + Bahasa news**:
  - 20 IDX blue-chip tickers added to catalog (`routers/stocks.py`) under new `IDX` exchange bucket — BBCA.JK, BBRI.JK, BMRI.JK, BBNI.JK, TLKM.JK, ASII.JK, UNVR.JK, GOTO.JK, ICBP.JK, INDF.JK, KLBF.JK, SMGR.JK, ADRO.JK, PTBA.JK, ANTM.JK, GGRM.JK, HMSP.JK, EXCL.JK, JSMR.JK, BREN.JK.
  - `services/idx_news.py` — new RSS scraper for CNBC Indonesia Market + Detik Finance (10-min TTL cache, word-boundary ticker+alias matcher with Bahasa company-name aliases). Returns same shape as Finnhub's `get_company_news()` so UI/PDF don't need to branch.
  - `services/sentiment.py` — shared Neulab keyword classifier extended with ~80 Bahasa Indonesia words (melonjak/anjlok/cuan/laba/rugi/dilaporkan/digugat etc.). Finnhub service now delegates to this shared module.
  - `routers/analysis.py` — `.JK` tickers route through `get_market_context_idx()` instead of Finnhub. Non-IDX tickers unchanged.
  - IDR currency formatting: `formatPrice` (frontend) uses `id-ID` locale (`Rp 6.450`), `_fmt_price` (PDF) uses `Rp ` with zero-decimal precision. Also covers JPY/KRW/VND edge cases.
  - Verdict-page header already shows `{exchange} · {currency}` ⇒ IDX stocks render as `JKT · IDR` with no extra code.
  - Live validation: `POST /api/analysis/BBCA.JK?mode=hybrid` → HOLD @ 62% conf, price Rp 6,450, target Rp 7,100, stop Rp 6,200; yfinance quotes/fundamentals, IDX news source populated (0 articles for BBCA this window is expected — graceful empty state).
  - Limitations: Finnhub's paid-only IDX quotes/consensus/earnings are intentionally skipped on free tier. News coverage depends on feed freshness and whether the ticker/company is mentioned by headline in the last ~100 items per source.

- **Apr 22 — PayPal smoke test + cancel endpoint (admin diagnostic)**:
  - `/admin/paypal-smoke-test` page + `GET /api/billing/smoke-test/plan` / `POST /api/billing/smoke-test/activate` / `GET /api/billing/smoke-test/history` / `POST /api/billing/smoke-test/cancel/{sub_id}`.
  - $1/mo Live PayPal plan (`P-4G394907YL6715359NHUNGJY`) created lazily and cached. End-to-end proven with a real Wise card purchase (`I-BG1V3KRJVK5U`, ACTIVE → CANCELLED after verification).
  - Prominent "Cancel smoke test" banner + per-row Cancel button in Recent diagnostics table.

- **Apr 22 — Sentiment transparency + roadmap on Why Us + CI**:
  - **Finnhub sentiment now transparent**: every headline carries `sentiment_triggers` (positive/negative keyword lists), and the payload includes a `daily_sentiment` 8-point sparkline (today + 7 prior days) built from the full article window.
  - **"Why this sentiment?" tooltip** on each headline — hover the sentiment badge to see matched trigger words (POS/NEG chips) with the "Neulab keyword heuristic" footer.
  - **7-day sentiment sparkline** rendered in the Headlines module header next to the overall score — SVG line + colored dots (green/red/amber) with native-tooltip hover detail per day.
  - **Why Us — roadmap modules added**: `WhyUsPage.jsx` grid expanded from 5 → 8 cards:
    - Watchlists & Alerts copy rewritten to explicitly mention @neulab_bot Telegram push alerts.
    - 3 new "Coming soon" blocks: **Autonomous AI Trading** (broker-connected autopilot), **Native Mobile Apps** (iOS + Android with Touch/Face ID), **SMS · CSV · Backtesting** (SMS alerts, watchlist CSV import/export, 12-month backtesting lab).
  - **CI: Finnhub Contract GitHub Action** (`.github/workflows/finnhub-contract.yml`):
    - `unit` job — PR blocker, runs the 6 offline sentiment/is_configured tests.
    - `integration` job — nightly (02:15 UTC) + on push to main, runs full 16-test contract against the preview backend via `PREVIEW_BACKEND_URL` secret.
  - **Pytest regression locked in** (`backend/tests/test_finnhub_contract.py`): 16 tests — 6 offline + 10 integration incl. trigger-word shape, 8-point sparkline bounds, PDF download contract. 1 LLM call per run via module-scoped fixture.

- **Apr 22 — Finnhub market context surfaced in UI + PDF**:
  - `MarketContextModules.jsx` renders 3 new modules on every Analysis Report between Candlestick Findings and Technical/Fundamental/Peer:
    - **Recent Headlines** — top 5 news items (last 7 days) with source, timeAgo, per-article sentiment chip (pos/neg/neutral), and click-out link; aggregate sentiment summary + score (-1..+1) in the header.
    - **Wall Street Consensus** — stacked bar of Strong Buy / Buy / Hold / Sell / Strong Sell, total analyst count, weighted score, and label (BUY / OVERWEIGHT / HOLD / UNDERWEIGHT / SELL). Graceful empty state when no coverage.
    - **Next Earnings** — date, days-until countdown (amber if ≤7 days), BMO/AMC/DMH label, quarter/year, EPS estimate, revenue estimate.
  - **PDF export** extended (`services/pdf.py`) — same 3 sections appear in the branded PDF under a "Market Context" header (headlines table, consensus row table, earnings summary line).
  - **Data Sources footer** (web + PDF) rewritten: live quotes & market context via Finnhub.io; fundamentals/history via yfinance; news sentiment via Neulab keyword heuristic; AI reasoning via Claude Sonnet 4.5.

## 1b. Recent Changes (Feb 2026)

- **Feb 22 — Live Desk guide + full free-tier candlestick**:
  - **Live Desk Guide** (`LiveDeskGuide.jsx`): New dismissable 4-step onboarding banner on Dashboard — (01) Pick a mode → (02) Click Analyze → (03) Read the verdict → (04) Export or share. Persists dismissal in localStorage.
  - **Watchlist Pattern Scan is now FREE** for all tiers (deterministic detector, no LLM). Removed the Pro gate on `/api/patterns/scan` + Dashboard's Scan Patterns button no longer shows the Pro lock.
  - **Pricing page**: Updated Free tier card to show checkmarks on Candlestick/Hybrid modes AND Watchlist pattern scan. Quick batch sweep (Top/Bottom 3) remains Pro (LLM-gated).
  - **Why Us page**: Refreshed competitive matrix — 17 rows now including Three analyzer modes, 15 candlestick patterns, Daily+weekly scan, Portfolio P&L, PDF export, Telegram push alerts, and "Candlestick & Hybrid on free tier" — with competitor comparison updated accordingly.
- **Telegram LIVE (Feb 22)**: `neulab_bot` is operational. Pattern alerts + high-conviction verdict alerts (BUY/SELL with conf ≥75) auto-push to linked chats.
- **Candlestick modes now FREE for all tiers (Feb 22)**: Removed the Pro/Elite gate on `mode=candlestick|hybrid` in `/api/analysis/{ticker}`. Pricing matrix + feature bullets updated — all 3 modes available to Free users. Pattern scan (bulk watchlist) remains a Pro/Elite perk.
- **Pattern Reference Guide (Feb 22)**: New `PatternGuideDialog` component (accessible via "Learn about patterns" button inside the Candlestick Findings section of any verdict). Displays all 15 supported patterns with Shape / What it means / How to read it / Best when. Searchable + bias-filterable. Patterns detected in the current verdict are highlighted and surface first.
- **Phase 2 — Portfolio, PDF, Telegram (Feb 22)**:
  - **Portfolio P&L** (`/portfolio`): New DB collection `portfolio`. Full CRUD at `/api/portfolio`. Computes live cost basis, market value, unrealized P&L (+ %), day change (+ %), allocation % per position using yfinance quotes. Add/Edit/Delete inline. Ticker deep-links to Analysis Report. Date picker uses shadcn Calendar + Popover.
  - **PDF Export**: `GET /api/analysis/{analysis_id}/pdf` returns a branded PDF via `reportlab`. Includes verdict, reasoning, candlestick findings (Daily/Weekly tables + Primary/Confirmation/Rejected summary), risks, and disclaimer. Owner-scoped (404 for cross-user). "Export PDF" button on AnalysisReportPage.
  - **Telegram Alerts**: New `TELEGRAM_BOT_TOKEN` + `TELEGRAM_BOT_USERNAME` env vars. Routes: `/api/telegram/status|link|poll|unlink|test`. User generates a 6-digit code in Settings, sends to the bot, frontend polls `/telegram/poll` which runs `getUpdates` and auto-links any matching code. Pattern alerts now push to linked chats via fire-and-forget `send_alert_to_user()`. Works out-of-the-box once admin provides bot token — currently shows "Bot not configured" banner gracefully.
  - Nav: added **Portfolio** and **Settings** to main AppShell menu.
  - Dependencies: added `reportlab==4.4.10` for PDF generation.
- **UI polish (earlier Feb 22)**: Analysis Report mode selector disabled on existing verdicts. Why Us cleanup. Pricing updated. Admin list shows analyses/day + ∞ for admins/unlock.
- **Pattern Alerts**: `POST /api/patterns/scan`
  - Analysis Report page: mode selector is now **disabled when viewing an existing verdict** — all three pills are locked, the active pill reflects `analysis.mode`, cursor is `not-allowed`, and the label changes from "Mode" to "Mode used" with a description stating the verdict's mode. Re-analyze preserves the same mode. To run a different mode, user returns to Dashboard and picks it there.
  - Why Us page: removed 4 coming-soon modules (Catalyst Radar, Risk Guard, Market Pulse, Screener Studio). Added new **Candlestick Strategy** module that mentions the 3 analyzer modes (Standard AI / Candlestick / Hybrid) with a link to pattern detectors, timeframes, and Claude reasoning.
  - Pricing page: added 3 new bullet points per tier — "Standard AI analysis mode" (always on), "Candlestick & Hybrid analysis modes" (Pro/Elite), "Watchlist pattern scan (15 patterns)" (Pro/Elite). Matrix shows strike-through on Free, check on Pro/Elite.
  - Admin users table: added **Analyses / day** column showing `used / limit`. Admin users and active test-unlock users render as `used / ∞` (amber) to reflect effective Elite tier.
  - Analysis Report `canPro` gate now respects `user.is_admin` and `user.test_unlock_expires_at` so admin/unlock accounts see candlestick modes as unlocked.
  - Backend `GET /admin/users` now returns `analyses_today`, `analyses_day_limit`, `effective_plan`, `test_unlock_active` per row.
  - AuthCallback: improved error copy + idempotency guard (skips exchange if JWT already present) to prevent double-consumption of the single-use Google session_id under React StrictMode.
- **Pattern Alerts**: `POST /api/patterns/scan` — one-click watchlist-wide candlestick scanner (Pro/Elite). Scans daily + weekly candles across every watchlist stock. Creates alerts for patterns with strength ≥ 70. Idempotent. No LLM calls. New "Scan Patterns" button in Dashboard quick actions.
- **Analysis Modes**: The Analyze Now engine now supports three modes selectable from the Dashboard and the Analysis Report page:
  - **Standard** (Mode A) — existing AI analysis using technicals, fundamentals, momentum. Available to all users (default for Free).
  - **Candlestick** (Mode B) — AI verdict driven primarily by detected candlestick patterns. *Pro/Elite only.*
  - **Hybrid** (Mode C, ★ default for Pro+) — AI fuses fundamentals + technicals + candlestick patterns, using patterns as timing/confirmation signals. *Pro/Elite only.*
  - Backend: `POST /api/analysis/{ticker}?mode=standard|candlestick|hybrid`. Pure-Python detector `services/candlestick.py` scans 15 common patterns (Doji, Hammer, Inverted Hammer, Shooting Star, Hanging Man, Bullish/Bearish Engulfing, Morning/Evening Star, Harami, Three White Soldiers / Black Crows, Piercing Line, Dark Cloud Cover, Tweezer Top/Bottom) on both **daily and weekly** timeframes and returns bias, strength, explanation per pattern + combined bias score.
  - UI: New `AnalysisModeSelector` component (3-pill group, Pro-gated) + new `CandlestickFindings` section on Analysis Report showing Daily/Weekly pattern columns + Primary/Confirmation/Rejected pattern summary from the AI.
  - Tests: `backend/tests/test_candlestick.py` (11/11 passing).
- Scorecard **timeframe filter** (7 days / 1 month / 3 months). Backend `/api/scorecard/me` and `/api/scorecard/global` accept `?timeframe=7|30|90` overriding `min_age_days`.
- WhyUs module rename: **"Alpha Score" → "Score Card"** (clickable — routes to `/scorecard`).
- WhyUs module rename: **"Explain Panel" → "AI Explain Panels"** with new description referencing confidence % + short/medium/long-term fit.
- WhyUs "How it works" step 02 updated: "composite Score Card ratings".
- **PayPal LIVE webhook** registered and verified reachable: `PAYPAL_WEBHOOK_ID=8LP24872BC452025J`. Simulator events fail signature (known quirk); real events will verify correctly.

## 2. Tech Stack

| Layer | Tech |
|---|---|
| Backend | FastAPI · MongoDB (Motor) · yfinance · emergentintegrations (Claude Sonnet 4.5) · httpx · PyJWT · bcrypt · Resend |
| Frontend | React 19 · Tailwind · shadcn/ui · recharts · lucide-react · react-router-dom · @paypal/react-paypal-js |
| Auth | JWT email/password + Emergent Google OAuth |
| Design | "Old Money Tech" — dark-first, Cormorant Garamond + Outfit + IBM Plex Mono |

## 3. Architecture

```
/app/
├── backend/
│   ├── core/            # config, db, models, security (JWT + admin elevation + auto-downgrade)
│   ├── routers/         # auth, plans, stocks, watchlist, analysis, admin, scorecard,
│   │                    # disclaimer, billing
│   ├── services/        # ai, quota (resolved limits), pricing, paypal, email, yfinance_svc
│   ├── tests/           # iteration1..iteration8 + backend_test
│   └── server.py
├── frontend/src/
│   ├── pages/           # Dashboard, AnalysisReport, Login, Signup, Pricing, PublicVerdict,
│   │                    # AuthCallback, Admin, Scorecard, WhyUs
│   ├── components/      # AppShell, AddStockModal, TimelineFitModal, DisclaimerModal,
│   │                    # VerdictRing, SignalBadge, ShareVerdictButton, Sparkline, ui/*
│   ├── context/         # AuthContext (memoized), ThemeContext (memoized)
│   └── lib/             # api, errors, format
└── memory/              # PRD.md + test_credentials.md
```

## 4. Key API Endpoints

- **Auth**: `POST /api/auth/register`, `/login`, `/google/session`, `GET /me`
- **Plans + Quota**: `GET /api/plans` (live prices + limits), `GET /quota`, `POST /plan/upgrade`
- **Stocks**: `GET /stocks/search` (category + exchange filters), `/stocks/exchanges`, `/stocks/{t}/quote`, `/stocks/{t}/history`
- **Watchlist**: `GET/POST/DELETE /api/watchlist`, `GET /watchlist/live` (batched aggregation)
- **Analysis**: `POST /analysis/{ticker}`, `POST /analysis/quick/{kind}` (bg-job), `GET /analysis/job/{id}`, `POST /analysis/{id}/share`, `GET /analysis/share/{id}` (public), `POST /analysis/timeline/{ticker}` (Pro+), `GET /analysis/timeline/{t}/latest`
- **Disclaimer**: `GET /disclaimer`, `POST /disclaimer/accept`
- **Billing**: `GET /billing/config`, `POST /billing/activate`, `POST /billing/cancel`, `POST /billing/webhook`
- **Admin**: `GET /admin/users`, `/logins`, `/pricing`, `/tier-limits` · `PUT /admin/pricing`, `/tier-limits` · `POST /admin/users/{id}/unlock`, `/reset`, `/users/delete` (bulk), `/logins/delete` (bulk) · `DELETE /admin/users/{id}`, `/users/{id}/alerts`, `/logins`
- **Scorecard**: `GET /scorecard`

## 5. Data Model

- `users`, `watchlist`, `analyses`, `timeline_recos`, `shared_verdicts`, `quick_jobs`, `alerts`, `disclaimers`, `subscriptions`, `webhook_events`, `login_events`, `settings` (pricing + tier_limits + PayPal plan IDs)

---

## 6. Feature Log

### ✅ Iteration 1 — MVP (shipped + deployed)
Watchlist, Claude verdict engine, in-app alerts, dashboard, detailed report, JWT auth

### ✅ Iteration 2 — Neural rebrand + tiers (shipped + deployed)
Lucid → Neural rebrand · Emergent Google OAuth · Free/Pro/Elite tiers · Share Verdict

### ✅ Iteration 3 — Admin + Scorecard (shipped + deployed)
Backend refactor (core/services/routers) · Admin auto-elevation (ADMIN_EMAILS) · Admin console (users, logins, unlock/reset) · AI Accuracy Scorecard

### ✅ Iteration 4 — Bg jobs + disclaimer (shipped + deployed)
Background-job quick-analyze + mandatory disclaimer gate

### ✅ Iteration 5 — PayPal + Resend + Admin (deployed)
PayPal sandbox subscriptions · Resend receipts · Admin dynamic pricing · Admin user delete · Admin alert-list clear · Share rate limit (5/50/∞) · Plan upgrade gating

### ✅ Iteration 6 — Yearly billing (deployed)
Yearly plans + 20% annual discount · Admin-configurable discount · Bulk login-event delete · "Top/Bottom 3" rename

### ✅ Iteration 7 — Timeline Fit (deployed)
Pro/Elite · Claude scores short/medium/long-term horizons · 24h cache · Modal UI with best-fit highlighting

### 🚀 Iteration 8 — Pending deploy (READY, not yet live)
*(tested locally, lint clean, ready to click Deploy)*
- **Cancellation grace period** — `/billing/cancel` keeps access until `next_billing_time`, auto-downgrade on expiry via `get_current_user`
- **Admin tier-limits editor** — configure analyses/day, analyses/week, shares/day per tier; blank = unlimited
- **Bulk user delete** — `POST /admin/users/delete` with PayPal cancellation cascade + paid-subscriber warning UI
- **Paid-subscriber warning banner** in admin before destructive delete
- **Expanded stocks catalog** — 90+ curated tickers across NASDAQ/NYSE/NYSEARCA/LSE/SGX/HKEX/TSE/TSX · category + exchange filters · "+N more exchanges" toggle · dynamic result caption
- **Why Us page** (`/why`) — product pitch + 10×7 competitive matrix vs moomoo/Tiger/TradingView/StashAway/TradeIdeas/DBSVickers · persuasion architecture (loss aversion, authority anchor, transparent scorecard) · trademarked branding
- **Test-unlock / admin banner** on pricing page explaining all-features-unlocked state
- **Cancellation banner** on pricing page showing cancels-at date
- **Elite card now renders PayPal Subscribe + Card buttons** (same as Pro) when not an active paid subscriber
- **Dashboard copy** — "Three positions" · "Analyze Now" (nbsp) · "Add up to three symbols"
- **AppShell header** — "Powered by Neural Labs Inc"
- **TimelineFitModal** — "AI RECOMMENDATION" label
- **Feature matrix alignment** — check/X icons right-aligned
- **Code review follow-ups** — memoized AuthContext + ThemeContext values, hoisted recharts config, fixed 3 empty catch blocks, fixed 5 array-index React keys
- **Removed `.env` from .gitignore** — required for deploy
- **N+1 removal in `/watchlist/live`** — single `$group` aggregation

## 7. Deployment Priority

🔴 **P0 — Deploy now**
1. All of Iteration 8 (listed above) — tested, lint clean, approved by deployment agent

🟡 **P1 — Set up before real-money launch**
2. Register PayPal webhook URL at `POST /api/billing/webhook` in PayPal Dashboard
3. Set `PAYPAL_WEBHOOK_ID` in `/app/backend/.env` — without this, recurring renewal receipts and lifecycle mutations stay in log-only mode
4. Flip `PAYPAL_ENV=live` when ready for real money (currently `sandbox`)

🟢 **P2 — Nice to have**
5. Verify custom domain in Resend → replace `onboarding@resend.dev`
6. Mobile responsiveness audit (tested desktop thoroughly, mobile not regression-tested)

## 8. Pending / Backlog (deferred per user)

### Phase 2 (deferred Feb 2026)
- Portfolio P&L tracking + CSV import/export
- Backtesting engine
- PDF export of analysis reports
- SMS / Telegram alert channels
- NewsAPI sentiment integration

### Phase 3 (future)
- Multi-asset support (crypto, forex)
- Brokerage execution integration
- Public developer API
- White-label / team plans
- Referral / "free month" mechanic

## 9. Testing Log

| Iter | Score | Notes |
|---|---|---|
| 1 | 35/39 | non-AI 100%; LLM budget blocked |
| 2 | 59/60 | 98.3% |
| 3 | 82/84 | 97.6% |
| 4 | 13/13 | 100% |
| 5 | 22/22 | 100% |
| 6 | 14/14 | 100% |
| 7 | 14/14 backend + 12/12 frontend | 100% |
| 8 | Manual smoke tested · pending full regression on prod | Ready to deploy |
| 9 | Manual smoke (Feb 2026) — scorecard timeframe filter + Why Us module rename verified | ✅ |
| 10 | 6/6 backend + 11/12 frontend — Analysis Modes (standard / candlestick / hybrid) end-to-end | ✅ zero critical issues |

## 10. Known Non-Blockers

- `PAYPAL_WEBHOOK_ID` unset — webhook events logged to `db.webhook_events` but don't mutate state. Lifecycle fallbacks handled by on-activation + auto-downgrade. Register before going live.
- No TTL on `quick_jobs` or `webhook_events` collections — low priority; volume is small.
- First `/billing/config` call is cold (~10-15s) because it lazy-creates 4 PayPal plans. Subsequent calls are fast.
- Resend still using `onboarding@resend.dev` sender — emails only deliver to verified address in Resend test mode. Verify custom domain before going wide.
- Sliding 24h share window (not calendar day) — acceptable for MVP.
- Auth uses JWT in localStorage. Architectural, not a bug. Future migration to httpOnly cookies is a separate P2 task.

## 11. Test Credentials

See `/app/memory/test_credentials.md`.

Primary test accounts:
- `jolor69@gmail.com` / `admin1234` — admin, auto-elevated to Elite via ADMIN_EMAILS
- `tiers@demo.io` / `pass1234` — regular Pro user
- `demo@stockai.io` / `demo1234` — Free user

---

*This document is the single source of truth for product scope. Update before each deploy.*
